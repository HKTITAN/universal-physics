# Implementation Plans

Generated by the `improve` skill on 2026-06-19 (planned at commit `71d585f`), scoped to the two standing blockers to a universal theory and what it takes to make the posed open problem solvable by outside experts. The analytical program is terminally saturated; these plans do **not** try to solve the physics — they harden the project so an external mathematician can pick up the carrier-problem dossier, and so the wiki can safely accept outside contributions.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Link integrity + a link-checker + CI | P1 | M | — | DONE |
| 002  | A contribution front-door for the carrier problem | P1 | S | 001 | DONE |
| 003  | Citation-integrity sweep (`[unverified]` ledger) | P2 | M | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (rationale)

## Dependency notes

- 002 lightly depends on 001 (the CI link-check should be green before advertising the contribution pathway, so a first outside PR sees a passing check).

## Findings considered and rejected

- *Rewrite the dossier* — rejected: it is already self-contained and submission-grade (it states Lemma N1/N2 inline, the precise conjecture, the residual, the five routes, the adjacent problems and the geometric input each smuggles). Only the surrounding wiring (002) needs work, not the dossier body.
- *Convert markdown links to Obsidian `[[wikilinks]]`* — rejected by the iteration-12 vault audit: the markdown-link convention is deliberate (works on the published site) and internally consistent. Do not mass-convert.
- *Regenerate `site/content/`* — rejected: it is regenerated from the vault at build time by `site/scripts/prepare-content.mjs`; the vault is the single source of truth.
