# Current State of the Campaign (through iteration 26, 2026-07-12)

The live snapshot every agent must load before working. The authoritative record
is the wiki (`../../FINDINGS.md`, `../../CHANGELOG.md`, `../../ROADMAP.md`); this
skill is the compressed orientation. When shared memory and this file disagree,
the wiki wins; update both.

## The standing verdict (unchanged 25 consecutive refereed iterations)

PARTIAL coherence · the leading route (spacetime from operator-algebraic/modular
data) **encodes** geometry but does not **generate** it · not-yet-physics (no
distinguishing experiment even in principle) · not forecastable. Principal hedge
HYP-CKV-VACUITY at **R7**: conditional HIGH on {n₁ AND (E_O)}.

## The distance ladder (what stands between here and a universal theory)

1. **The free-field gate** — after iteration 25, in NORMAL FORM:
   - **P1-NF: ESTABLISHED** (iteration 25, assembler cold re-derivation): the
     coupled strip system is EXACTLY the scalar imaginary-AB-flux problem
     H(iτ₀)g = 0 on the cylinder via the explicit T∘W. The gate = the
     analytically-continued positive flux family has no L² zero mode.
     LEM-A1‴-T subsumed; Bari conditionality DISCHARGED; complex-c extension
     ESTABLISHED; the a = 2 ceiling RETRACTED (artifact — the mode recast is
     a coupled LOCAL system).
   - **After iteration 26:** S5 DISCHARGED (repaired slacked form; the
     un-slacked skeleton fails Θ(n) — structural); {LEM-A1⁗-N} ⟺ LEM-A1⁗
     now ESTABLISHED; LEM-TRACE ESTABLISHED (seam identity as written,
     resonant-log caveat discharged, μ = e^{2πτ₀}); seam chain graded
     (B0 + B1-free ESTABLISHED via Schoenberg positivity-improving;
     B2/B3 INFERENCE). **THE residual crux: LEM-SEAM-SC [OPEN]** — no
     SIGN-CHANGING embedded seam eigenfunction (one-signed case dies on
     the Perron chain) — plus branch-(b) rate promotion [OPEN], CH-26(iii)
     (the b → 0 channel closure: A₀-LAP + transversality), T1, SEAM-ESS +
     the B1 write-up. Numerics: 369-point global shell closure (sr ≤ 0.006
     at 2 units); support [INFERENCE, medium]. Compact windows suffice.
   - Proving both ⟹ (E_O) for the free massive scalar ⟹ **R7 → R8** (the no-go
     becomes a theorem on that subclass). Established upstream: the E=0 half
     (FG-1989 + LEM-K0), the Corner Indicial Theorem (τ₀ = ε/2π), the strip
     geometrization (Q = m²R²(cosh ξ − cos η)⁻²), the c-collapse
     (gate ⟺ σ_p(A) ∩ (1,∞) = ∅, A = S_I^{1/2}R_I S_I^{1/2}), the conditional
     Mourre theorem (explicit conjugate D_g, i[A₀,D_g] = 1).
2. **Interacting nets** — no known technology (proved undecidable at invariant
   level, iteration 17).
3. **n₁ (the causal order)** — presupposed by every known algebraic seed
   (iterations 18/20); no derivation route.
4. **The carrier** (flip encodes→generates) — five routes converge and halt;
   25 consecutive confirmations say the wall is real (iteration 25: the
   screen reproduces in explicit mode coordinates; common-cause theorem).
5. **A distinguishing experiment** — none exists even in principle (the encoding
   screen); nearest dated external event: 2027 DESI five-year w(z) (tests the
   *target*, not the wager).

## Proved boundaries on methods (do NOT retry these)

- No family-level/soft theorem (BKT: embedded eigenvalues occur in the
  finite-interval-convolution family).
- No decay/analyticity-class theorem (the Gaussian rank-one BIC counterexample —
  five lines; Kato–Agmon–Simon is irreducibly local).
- No pointwise-positive Krein symmetrizer (the unique one is indefinite).
- No global definitizability (infinitely many non-real essential branches for
  every c > 1); local type-π₋ Pontryagin structure is conditional and open.
- The Krein/indefinite-metric carrier route, the bicentralizer route, and
  invariant-level arguments for (E_O): all closed (iterations 12, 17).

## Designated next moves (in order)

1. SEAM-ESS + the B1 Hopf/maximum-principle write-up — unblocks B0's Perron
   corollary to ESTABLISHED and hardens the B2/B3 chain.
2. Branch-(b) rate promotion via the proven slacked-Volterra machinery
   (double payoff: completes the dichotomy AND discharges B3's seam-trace
   hypothesis for gate candidates).
3. Pin T1 (cylinder Allegretto-1974/Piepenbrink-1974 at threshold); read
   Aydi–Kchaou–Trunk 2607.05852 + Il'yasov–Valeev 2606.31129 first.
4. CH-26(iii): A₀-LAP in the shell channel + quantitative transversality
   (the b → 0 closure the lattice cannot probe).
5. Housekeeping: KWSS cylinder localization; slacked-space restatement in
   registries; TP route only after B1/SEAM-ESS (τ₀-confound pre-named).
6. Watch clocks: (α)–(ε) operator-algebra items; DESI/Euclid/Roman.

## Where everything lives

- Registries: `HYPOTHESES.md` (hedge thread), `FINDINGS.md` (iteration updates),
  `CHANGELOG.md` (canonical log), `ROADMAP.md` (watch state).
- The mathematics notes: `notes/2026-07-*-iter1[6-9]*.md`, `notes/2026-07-11-iter2[0-4]*.md`,
  `notes/2026-07-12-iter2[5-6]*.md`.
- The numerics (reproducible, all probes documented incl. failed designs):
  `scripts/eo-modular-numerics.py`.
- The posed problems for external solvers: `notes/2026-06-19-iter15-carrier-problem-dossier.md`.
